using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Settings
{
    public const int maxChildCorridors = 3; // max number of child corridors leading from a room - maximum should be 3 through this is not recommend since it can cause the dungeon buildings to fail since the rooms are more likely to not fit together;
    
}
